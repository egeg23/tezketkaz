// Phase 13.1.5 — Legal acceptance versioning (mobile side).
//
// Mirror of `backend/src/constants/legal.js`. Bump this string in lockstep
// with the backend's `CURRENT_LEGAL_VERSION` whenever the T&C / Privacy
// Policy receive a material update that requires re-acceptance.

const String kCurrentLegalVersion = 'v1.0.0';
